# Agent Board

## Active tasks

| Agent | Task | Branch / Worktree | Files claimed | Status | Notes |
|-------|------|-------------------|---------------|--------|-------|
| mobile-agent | Expo SDK 54\u219257 upgrade | agent/mobile/expo-sdk-57-upgrade | packages/mobile/** | done, awaiting device/EAS testing before merge | Dedicated branch per user request. Ran `npx expo install expo@~57.0.18 --fix` then fixed fallout: (1) expo-router 57 completely dropped @react-navigation/native/bottom-tabs/native-stack as dependencies in favor of its own `standard-navigation` package \u2014 6 screens importing `useFocusEffect` from `@react-navigation/native` were switched to import it from `expo-router` (which now re-exports useFocusEffect/useNavigation/useIsFocused/ThemeProvider etc. directly); (2) removed stray root-level `@expo/config-plugins`/`react-native-safe-area-context`/`prop-types` deps that were conflicting with the mobile package's own versions (`prop-types` already declared correctly in packages/frontend); (3) added `typescript: ~6.0.3` as a mobile-local devDependency (SDK 57 expects TS6, backend/frontend keep their own separate 5.x pins so unaffected); (4) deleted stale unused `packages/mobile/app.json` (dynamic `app.config.js` never read it, and its top-level `splash` schema is invalid in SDK 57); moved splash config into the `expo-splash-screen` plugin config in app.config.js; (5) untracked `.expo/` (was accidentally committed, now gitignored at both root and packages/mobile). `npx expo-doctor` now 20/21 (last item is an expected/inherent monorepo trade-off: frontend pins React 18, mobile pins React 19 \u2014 correctly isolated by npm workspaces, not a real bug). `npx tsc --noEmit` clean. Web smoke test (`expo start --web`, CI=1) bundled successfully (1129 modules, HTTP 200). **NOT verified**: real iOS/Android device or EAS build \u2014 not possible in this sandboxed session. Do not merge to dev without that verification; Expo Go for SDK 57 itself isn't yet on the App/Play Store per Expo's own SDK 57 changelog (`eas go` or dev builds required). |

## Done

| Agent | Task | Branch / Worktree | PR / Commit | Notes |
|-------|------|-------------------|-------------|-------|
| vendor-agent | Vendor mobile V1: dashboard shell + profile view/edit | agent/mobile/vendor-v1 | not committed | Files: app/(vendor)/{_layout,index,profile}.tsx. Uses GET/PUT /vendors/account/me + GET /vendors/bookings/mine. `npx tsc --noEmit` passes. roleRouting.ts flipped to /(vendor). |
| artist-agent | Artist mobile A1: dashboard shell + profile + rider | agent/mobile/artist-v1 | not committed | Files: app/(artist)/{_layout,index,profile,rider}.tsx. Uses GET/PUT /artists/me/profile + /artists/me/rider (confirmed). `npx tsc --noEmit` passes. roleRouting.ts flipped to /(artist). |
| promoter-agent | Promoter mobile P1: dashboard shell + profile | agent/mobile/promoter-v1 | not committed | Files: app/(promoter)/{_layout,index,profile}.tsx, types/promoter.ts. Uses GET/PUT /promoter/profile (confirmed). `npx tsc --noEmit` passes. roleRouting.ts flipped to /(promoter). |
| owner-agent | Owner mobile O2: venues list/detail/create | agent/mobile/owner-o2-venues | not committed | Files: app/(tabs)/venues/{index,[id],new,_layout}.tsx, 1-line hidden-tab registration in app/(tabs)/_layout.tsx. No GET /owner/venues/:id exists — detail screen fetches the list and finds by id client-side. `npx tsc --noEmit` passes. |
| bootstrap (main session) | Fixed vendor/artist/promoter roles all mis-routing to owner dashboard | n/a | not committed | Added lib/roleRouting.ts (single source of truth for role→home-route), app/_layout.tsx consumes it instead of a hardcoded OWNER_ROLES array; registered (vendor)/(artist)/(promoter) as root Stack.Screen entries. |
| vendor-agent | Vendor V2: booking requests list + respond (accept/decline) | agent/mobile/vendor-v2 | not committed | Files: app/(vendor)/booking-requests/{index,_layout}.tsx, components/BookingRequestCard.tsx, types/vendorBooking.ts, nav link in app/(vendor)/index.tsx, Stack.Screen in app/(vendor)/_layout.tsx. Confirmed via direct read of vendors.controller.ts/vendors.service.ts: GET /vendors/booking-requests/mine returns vendor_booking_requests rows; PUT /vendors/booking-requests/:id body { status: 'confirmed'\|'declined'\|'cancelled', quotedAmount?, notes? }. `npx tsc --noEmit` passes. |
| artist-agent | Artist A2: bookings list (own + promoter) + respond | agent/mobile/artist-v2 | not committed | Files: app/(artist)/bookings/{index,[id],_layout}.tsx, app/(artist)/_layout.tsx, app/(artist)/index.tsx. artist-bookings (GET /artist-bookings/mine, /:id) is read-only, no respond endpoint; promoter-bookings respond via PATCH /promoter-bookings/:id/artist-respond body { action: 'accept'\|'decline' }. `npx tsc --noEmit` passes. |
| promoter-agent | Promoter P2: promoter-run events list/detail | agent/mobile/promoter-v2 | not committed | Files: app/(promoter)/events/{index,[id],_layout}.tsx, nav link in app/(promoter)/index.tsx, Stack.Screen in app/(promoter)/_layout.tsx. Confirmed via direct read of promoter-events.controller.ts: GET /promoter-events/mine and GET /promoter-events/:id return public_events rows + nested ticket_tiers, scoped to caller's own promoter_account. View-only — POST/PUT for create/edit exist but involve nested ticket-tier arrays, deferred to a later phase. `npx tsc --noEmit` passes. |
| owner-agent | Owner O3: vendors browse/book + manage vendor bookings | agent/mobile/owner-o3-vendors | not committed | Files: app/(tabs)/vendors/{index,[id],bookings,_layout}.tsx, 1-line hidden-tab registration in app/(tabs)/_layout.tsx. Confirmed via direct read of vendors.controller.ts/vendors.service.ts: GET /vendors/public returns { vendors, venues }; POST /vendors/bookings body { vendorAccountId, eventName, eventDate, notes? } (CreateVendorBookingDto); GET /vendors/bookings/owner returns an array; PUT /vendors/bookings/:id (status cancel) is authorized for the original booker (owner), not vendor-only as the agent had assumed — verified safe. `npx tsc --noEmit` passes. |
| owner-agent | Owner O4: vendor invoices owed (view-only) | agent/mobile/owner-o4-vendor-invoices | not committed | Files: app/(tabs)/vendor-invoices/{index,[id],_layout}.tsx, 1-line hidden-tab registration in app/(tabs)/_layout.tsx. Confirmed via direct read of vendor-invoices.controller.ts/service.ts: GET /vendor-invoices/owner-bookings + GET /vendor-invoices/owner/:id return vendor_invoices rows (total_amount, amount_due, amount_paid, subtotal, tax_rate, tax_amount, discount_amount) + vendor_invoice_items(*) + vendor_accounts(business_name, ...). View-only, no pay/edit actions this phase. `npx tsc --noEmit` passes. |
| vendor-agent | Vendor V3: bookings list/detail + status update | agent/mobile/vendor-v3 | not committed | Files: app/(vendor)/bookings/{index,[id],_layout}.tsx, nav link in app/(vendor)/index.tsx (added centrally), Stack.Screen in app/(vendor)/_layout.tsx. Uses GET /vendors/bookings/mine + PUT /vendors/bookings/:id (UpdateVendorBookingDto, camelCase). Quick actions: pending→confirm/decline, confirmed→complete/cancel (with confirm-before-cancel). `npx tsc --noEmit` passes. |
| artist-agent | Artist A3: invoices list/detail/create | agent/mobile/artist-v3 | not committed | Files: app/(artist)/invoices/{index,[id],new,_layout}.tsx, nav link in app/(artist)/index.tsx, Stack.Screen in app/(artist)/_layout.tsx. Uses GET/POST/PUT/DELETE /artist-invoices*, POST /artist-invoices/:id/send. Confirmed `vendor_booking_id` in the shared DTO shape is a copy-paste artifact — no artist-specific equivalent exists, omitted from the create form. `client_email` is actually required (not optional as originally briefed). `npx tsc --noEmit` passes. |
| promoter-agent | Promoter P3: browse + book artists | agent/mobile/promoter-v3 | not committed | Files: components/ArtistCard.tsx, app/(promoter)/artists/{index,[id],_layout}.tsx, app/(promoter)/bookings/{index,[id],_layout}.tsx, nav links in app/(promoter)/index.tsx (added centrally), Stack.Screen entries in app/(promoter)/_layout.tsx. Uses GET /artists/search (browse), GET /artists/:id, POST/GET/PUT /promoter-bookings* (snake_case body). Corrected: artists have no hourly_rate/flat_rate — actual fields are performance_fee_min/performance_fee_max. Fixed a removed React Navigation prop (headerBackTitleVisible → headerBackButtonDisplayMode) found during central validation. `npx tsc --noEmit` passes. |
| vendor-agent | Vendor V4: invoices list/detail/create (own clients) | agent/mobile/vendor-v4 | not committed | Files: app/(vendor)/invoices/{index,[id],new,_layout}.tsx, nav link + Stack.Screen in app/(vendor)/index.tsx + app/(vendor)/_layout.tsx (mirrors (tabs)/invoices conventions). Uses GET /vendor-invoices/mine, GET/PUT /vendor-invoices/:id, POST /vendor-invoices, POST /vendor-invoices/:id/send (confirmed to exist, "Send to Client" action added for draft invoices). Cleaned up an inline-styled Invoices nav card to match the shared navCard style during central validation. `npx tsc --noEmit` passes. |
| artist-agent | Artist A4: calendar view | agent/mobile/artist-v4 | not committed | Files: app/(artist)/calendar.tsx (hand-built month grid, no new dependency, mirrors (tabs)/calendar.tsx pattern), nav link + Stack.Screen in app/(artist)/index.tsx + app/(artist)/_layout.tsx. Combines GET /artist-bookings/mine + GET /promoter-bookings/for-artist, tap-to-expand day list, navigates to booking detail with a `source` query param. `npx tsc --noEmit` passes. |
| owner-agent | Owner O10: booking link settings + inbound requests (mobile) | agent/mobile/owner-o10-booking-link | not committed | Files: app/(tabs)/booking-link/{_layout,index}.tsx, hidden-tab registration in app/(tabs)/_layout.tsx. Settings card (slug/active-toggle/custom-message via POST /owner/booking-links, GET /owner/booking-links/mine) + inbound requests list (GET /owner/booking-links/requests, PUT .../requests/:id body { status: 'confirmed'\|'declined' }). Uses Share.share() instead of a clipboard package (no expo-clipboard dependency added) and https://dovenuesuite.com as the public link base (confirmed from backend CORS allow-list). Fixed a removed React Navigation prop (headerBackTitleVisible → headerBackButtonDisplayMode) found during central validation. `npx tsc --noEmit` passes. |
| promoter-agent | Promoter P4: browse + book vendors + manage vendor bookings | agent/mobile/promoter-v4 | not committed | Files: app/(promoter)/vendors/{_layout,index,[id]}.tsx, app/(promoter)/vendor-bookings/{_layout,index,[id]}.tsx, nav links + Stack.Screen entries in app/(promoter)/index.tsx + app/(promoter)/_layout.tsx. Built directly (subagent dispatch failed, falsely reporting the (promoter) route group didn't exist). Reuses VendorCard + Vendor type, GET /vendors/public ({ vendors, venues } — vendors only, defensive snake_case mapping), GET /vendors/:id, POST /vendors/bookings (camelCase CreateVendorBookingDto), GET /vendors/bookings/booked-by-me, GET/PUT /vendors/bookings/:id (status cancel). `npx tsc --noEmit` passes. |
| backend-agent | Security fix: guest-lists auth-guard + ownership scoping | main session | not committed | Files: src/guest-lists/{guest-lists.service.ts,guest-lists.controller.ts}. Added assertGuestListOwnership/assertGuestOwnership/assertOwnsEvent (404 NotFoundException, not 403, to avoid ID-enumeration confirmation) + findAllForOwner replacing the unscoped global findAll(). Every ID-based endpoint now requires the Authorization header and verifies the caller owns the parent event via event.owner_id. Left findByShareToken/findByAccessCode/validateAccess/findByArrivalToken public by design (guest self-service via unguessable token/code). `npm run build` clean; eslint delta (137→152 errors) confirmed via git stash comparison to be pre-existing `any`-typed-style debt, not a new category. |
| owner-agent | Owner dashboard nav links for Venues/Vendors/Vendor-Invoices/Booking-Link/Door-Lists | main session | not committed | File: app/(tabs)/dashboard.tsx — added 5 Quick Actions entries wired to the routes built in O2/O3/O4/O10/O6. `npx tsc --noEmit` passes. |
| owner-agent | Owner O6: Door/Guest Lists mobile (unblocked after guest-lists auth fix) | main session | not committed | Files: app/(tabs)/door-lists/{_layout,index,[id]}.tsx, hidden-tab registration in app/(tabs)/_layout.tsx, nav link in dashboard.tsx. index.tsx lists upcoming events + guest-list stats (create-on-demand via POST /guest-lists); [id].tsx is the check-in screen (stats, search/filter, add guest, import-from-RSVP, lock toggle, arrive/unarrive). `npx tsc --noEmit` passes. |
| vendor-agent | Vendor V5: booking link settings (requests reuse existing V2 screen) | main session | not committed | Files: app/(vendor)/booking-link/{_layout,index}.tsx, Stack.Screen + nav card in app/(vendor)/_layout.tsx + index.tsx. POST/GET /vendors/booking-links(/mine) incl. defaultDepositPercentage; public URLs /book/:slug and /b/:code. Links out to the existing Booking Requests screen (V2) rather than duplicating that list. `npx tsc --noEmit` passes. |
| artist-agent | Artist A5: booking link settings + requests (combined, no prior requests screen existed) | main session | not committed | Files: app/(artist)/booking-link/{_layout,index}.tsx, Stack.Screen + nav card in app/(artist)/_layout.tsx + index.tsx. POST/GET /artists/booking-links(/mine), GET/PUT /artists/booking-links/requests(/:id); public URLs /book-artist/:slug and /a/:code. `npx tsc --noEmit` passes. |
| promoter-agent | Promoter P6: invoices (own, full CRUD) + artist bills (read-only) | main session | not committed | Files: app/(promoter)/invoices/{_layout,index,new,[id]}.tsx (mirrors vendor/owner invoice CRUD pattern, promoter_invoice_items), app/(promoter)/artist-bills/{_layout,index}.tsx (GET /promoter-bookings/artist-invoices/mine, opens https://dovenuesuite.com/pay/invoice/:token via Linking for payment — no in-app Stripe checkout this phase), nav buttons in app/(promoter)/index.tsx, Stack.Screen entries in app/(promoter)/_layout.tsx. `npx tsc --noEmit` passes. |
| promoter-agent | Promoter P8: calendar (own events + artist bookings + vendor bookings) | main session | not committed | File: app/(promoter)/calendar.tsx (hand-built month grid, no new dependency, mirrors artist A4 pattern). Combines GET /promoter-events/mine + GET /promoter-bookings/mine + GET /vendors/bookings/booked-by-me via Promise.allSettled; color-coded by source; tap navigates to the respective existing detail screen. Nav button + Stack.Screen added in app/(promoter)/index.tsx + _layout.tsx. `npx tsc --noEmit` passes. |
| owner-agent | Owner O5: Contracts (view/send + owner-sign) | main session | not committed | Files: app/(tabs)/contracts/{_layout,index,[id]}.tsx, hidden-tab registration in app/(tabs)/_layout.tsx, nav link in dashboard.tsx. Added dependency `react-native-signature-canvas` (+ peer `react-native-webview`) via `npx expo install`, approved by user. GET /contracts (owner's own via ownerId), GET /contracts/:id, POST /contracts/:id/send, POST /contracts/:id/owner-sign. Shared components/SignaturePad.tsx wraps the canvas in a full-screen Modal. `npx tsc --noEmit` passes. |
| vendor-agent | Vendor V6: Contracts (view + vendor-sign) | main session | not committed | Files: app/(vendor)/contracts/{_layout,index,[id]}.tsx, Stack.Screen + nav card in app/(vendor)/_layout.tsx + index.tsx. GET /contracts/vendor/mine, GET /contracts/:id, POST /contracts/:id/vendor-sign, reuses components/SignaturePad.tsx. `npx tsc --noEmit` passes. |

## Blocked

| Agent | Task | Blocker | Needed decision |
|-------|------|---------|-----------------|
| mobile-agent | Upgrade Expo SDK 54 → 57 to clear remaining `npm audit` findings in `packages/mobile` (22 vulnerabilities: image-size, postcss, uuid — all transitive deps of Metro/@expo/cli/@expo/config-plugins build tooling, not shipped in the app JS bundle) | `npm audit fix --force` only fixes these by force-upgrading `expo` to 57.0.18, a breaking major-version bump also affecting expo-router, expo-notifications, expo-dev-client | User to decide when to schedule a dedicated SDK 57 migration (own branch, full iOS/Android regression + EAS build verification) — not to be done as a side effect of an audit fix |
| vendor/artist/promoter-agent | New role portals only reachable once a user actually has that role and an underlying account row (vendor_accounts/artist_accounts/promoter_accounts) — no "create profile" flow exists yet in mobile for any of the 3 roles if the row is missing | Needs dedicated onboarding/registration screens, or confirm web-only onboarding is acceptable for now | Product decision: is mobile self-serve account creation in scope, or is account creation always done on web first? |
| n/a | Promoter P5 (Contracts) | n/a | n/a | Confirmed via backend grep: no promoter/contract linkage exists anywhere in `packages/backend/src` (contracts are owner\u2194client and owner\u2194vendor only, via `client_id`/`vendor_account_id`). Dropped as not applicable \u2014 not a mobile-parity gap. |

